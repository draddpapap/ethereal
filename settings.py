# ============================================
# ⚙️ ОСНОВНЫЕ НАСТРОЙКИ
# ============================================

SHUFFLE_WALLETS = True  # Перемешивать кошельки в очереди
RETRY = 3               # Количество повторов при ошибках
THREADS = 1             # Количество одновременно работающих аккаунтов

# ============================================
# 💰 ТОКЕНЫ ДЛЯ ТОРГОВЛИ
# ============================================

TOKENS_TO_TRADE = {
    "BTCUSD": {
        "prices": [95000, 110000],      # Диапазон цен для торговли
        "leverage": [5, 10],            # Диапазон плеча
        "open_price": [0.0, 0.0],       # $ смещение от цены
    },
    "ETHUSD": {
        "prices": [3000, 4000],
        "leverage": [5, 15],
        "open_price": [0.0, 0.0],
    },
    "SOLUSD": {
        "prices": [150, 250],
        "leverage": [3, 10],
        "open_price": [0.0, 0.0],
    },
}

# ============================================
# 📊 ТИПЫ ОРДЕРОВ
# ============================================

FUTURE_ACTIONS = {
    "Long": True,   # Разрешить лонги
    "Short": True,  # Разрешить шорты
}

# Количество циклов (открытие+закрытие) на один аккаунт
TRADES_COUNT = [2, 4]

# ============================================
# 💵 РАЗМЕРЫ ПОЗИЦИЙ
# ============================================

TRADE_AMOUNTS = {
    "amount": [10, 50],     # $ сумма ИЛИ
    "percent": [50, 80],    # % от баланса (если amount = [0, 0])
}

# ============================================
# 📈 ЛИМИТ ОРДЕРЫ (Режим 2)
# ============================================

FUTURES_LIMITS = {
    "close_previous": True,              # Закрывать старые позиции перед новыми
    "price_diff_amount": [0.0, 0.0],    # $ профит-тейк
    "price_diff_percent": [0.05, 0.10], # % профит-тейк
    "to_wait": 2,                        # Минут ждать LIMIT ордер при открытии
}

# ============================================
# 🛑 СТОП-ЛОСС
# ============================================

STOP_LOSS_SETTING = {
    "enable": False,                     # Включить?
    "loss_diff_amount": [0.0, 0.0],     # $ стоп-лосс
    "loss_diff_percent": [0.08, 0.12],  # % стоп-лосс
}

# ============================================
# ❌ ОТМЕНА ОРДЕРОВ (Режим 4)
# ============================================

CANCEL_ORDERS = {
    "orders": True,      # Закрывать ордеры
    "positions": True,   # Закрывать позиции
}

# ============================================
# 🔗 ДЕЛЬТА-НЕЙТРАЛЬНАЯ ТОРГОВЛЯ (Режим 3)
# ============================================

PAIR_SETTINGS = {
    "pair_amount": [3, 3],      # Аккаунтов в паре
    "position_hold": [40, 120], # Секунд держать позицию
}

# ============================================
# ⏱️ ЗАДЕРЖКИ (секунды)
# ============================================

SLEEP_BETWEEN_OPEN_ORDERS = [3, 7]   # Между открытием в режиме 3
SLEEP_BETWEEN_CLOSE_ORDERS = [3, 7]  # Между закрытием в режиме 3
SLEEP_AFTER_FUTURE = [5, 15]         # После каждого ордера
SLEEP_AFTER_ACC = [10, 30]           # После каждого аккаунта

# ============================================
# 📱 TELEGRAM УВЕДОМЛЕНИЯ
# ============================================

TG_BOT_TOKEN = ''    # Токен бота (пусто = отключено)
TG_USER_ID = []      # ID пользователей [123456, 789012]

# ============================================
# 🌐 API ETHEREAL
# ============================================

ETHEREAL_API_URL = "https://api.ethereal.trade"  # Mainnet
# ETHEREAL_API_URL = "https://api.etherealtest.net"  # Testnet
